// Code generated by SQLBoiler 3.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSalts(t *testing.T) {
	t.Parallel()

	query := Salts()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSaltsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSaltsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Salts().DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSaltsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SaltSlice{o}

	if rowsAff, err := slice.DeleteAll(tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSaltsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SaltExists(tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Salt exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SaltExists to return true, but got false.")
	}
}

func testSaltsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	saltFound, err := FindSalt(tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if saltFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSaltsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Salts().Bind(nil, tx, o); err != nil {
		t.Error(err)
	}
}

func testSaltsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Salts().One(tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSaltsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	saltOne := &Salt{}
	saltTwo := &Salt{}
	if err = randomize.Struct(seed, saltOne, saltDBTypes, false, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}
	if err = randomize.Struct(seed, saltTwo, saltDBTypes, false, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = saltOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = saltTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Salts().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSaltsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	saltOne := &Salt{}
	saltTwo := &Salt{}
	if err = randomize.Struct(seed, saltOne, saltDBTypes, false, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}
	if err = randomize.Struct(seed, saltTwo, saltDBTypes, false, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = saltOne.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = saltTwo.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func saltBeforeInsertHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func saltAfterInsertHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func saltAfterSelectHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func saltBeforeUpdateHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func saltAfterUpdateHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func saltBeforeDeleteHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func saltAfterDeleteHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func saltBeforeUpsertHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func saltAfterUpsertHook(e boil.Executor, o *Salt) error {
	*o = Salt{}
	return nil
}

func testSaltsHooks(t *testing.T) {
	t.Parallel()

	var err error

	empty := &Salt{}
	o := &Salt{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, saltDBTypes, false); err != nil {
		t.Errorf("Unable to randomize Salt object: %s", err)
	}

	AddSaltHook(boil.BeforeInsertHook, saltBeforeInsertHook)
	if err = o.doBeforeInsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	saltBeforeInsertHooks = []SaltHook{}

	AddSaltHook(boil.AfterInsertHook, saltAfterInsertHook)
	if err = o.doAfterInsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	saltAfterInsertHooks = []SaltHook{}

	AddSaltHook(boil.AfterSelectHook, saltAfterSelectHook)
	if err = o.doAfterSelectHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	saltAfterSelectHooks = []SaltHook{}

	AddSaltHook(boil.BeforeUpdateHook, saltBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	saltBeforeUpdateHooks = []SaltHook{}

	AddSaltHook(boil.AfterUpdateHook, saltAfterUpdateHook)
	if err = o.doAfterUpdateHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	saltAfterUpdateHooks = []SaltHook{}

	AddSaltHook(boil.BeforeDeleteHook, saltBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	saltBeforeDeleteHooks = []SaltHook{}

	AddSaltHook(boil.AfterDeleteHook, saltAfterDeleteHook)
	if err = o.doAfterDeleteHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	saltAfterDeleteHooks = []SaltHook{}

	AddSaltHook(boil.BeforeUpsertHook, saltBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	saltBeforeUpsertHooks = []SaltHook{}

	AddSaltHook(boil.AfterUpsertHook, saltAfterUpsertHook)
	if err = o.doAfterUpsertHooks(nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	saltAfterUpsertHooks = []SaltHook{}
}

func testSaltsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSaltsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Whitelist(saltColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSaltsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(tx); err != nil {
		t.Error(err)
	}
}

func testSaltsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SaltSlice{o}

	if err = slice.ReloadAll(tx); err != nil {
		t.Error(err)
	}
}

func testSaltsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Salts().All(tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	saltDBTypes = map[string]string{`ID`: `int`, `CreatedAt`: `timestamp`, `UpdatedAt`: `timestamp`, `DeletedAt`: `timestamp`, `UserID`: `int`, `Salt`: `varchar`}
	_           = bytes.MinRead
)

func testSaltsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(saltPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(saltAllColumns) == len(saltPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, saltDBTypes, true, saltPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	if rowsAff, err := o.Update(tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSaltsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(saltAllColumns) == len(saltPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Salt{}
	if err = randomize.Struct(seed, o, saltDBTypes, true, saltColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, saltDBTypes, true, saltPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(saltAllColumns, saltPrimaryKeyColumns) {
		fields = saltAllColumns
	} else {
		fields = strmangle.SetComplement(
			saltAllColumns,
			saltPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SaltSlice{o}
	if rowsAff, err := slice.UpdateAll(tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSaltsUpsert(t *testing.T) {
	t.Parallel()

	if len(saltAllColumns) == len(saltPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLSaltUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Salt{}
	if err = randomize.Struct(seed, &o, saltDBTypes, false); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	tx := MustTx(boil.Begin())
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Salt: %s", err)
	}

	count, err := Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, saltDBTypes, false, saltPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Salt struct: %s", err)
	}

	if err = o.Upsert(tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Salt: %s", err)
	}

	count, err = Salts().Count(tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
